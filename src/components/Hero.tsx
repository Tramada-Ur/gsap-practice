import { ScrollTrigger, SplitText } from 'gsap/all';
import gsap from 'gsap';
gsap.registerPlugin(ScrollTrigger, SplitText);


const Hero = () => {
    return (
        <>
            <section id='#hero' className='hero'>
                <h1 className=''>
                    Learning Science Poured out for you
                </h1>


            </section>

        </>
    );
};
export default Hero;